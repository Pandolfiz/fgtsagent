<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Diagnóstico de URL OAuth2</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      font-family: 'Arial', sans-serif;
      line-height: 1.6;
      color: #333;
      padding: 20px;
    }
    
    .container {
      max-width: 900px;
      margin: 0 auto;
    }
    
    .card {
      margin-bottom: 20px;
      border-radius: 8px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }
    
    .card-header {
      font-weight: bold;
      background-color: #f8f9fa;
    }
    
    .correct {
      background-color: #d4edda;
      border-color: #c3e6cb;
      color: #155724;
      padding: 10px;
      border-radius: 5px;
      margin-bottom: 15px;
    }
    
    .incorrect {
      background-color: #f8d7da;
      border-color: #f5c6cb;
      color: #721c24;
      padding: 10px;
      border-radius: 5px;
      margin-bottom: 15px;
    }
    
    .code-block {
      background-color: #f8f9fa;
      border: 1px solid #eee;
      border-radius: 5px;
      padding: 15px;
      margin-bottom: 15px;
      overflow-x: auto;
      font-family: monospace;
      white-space: pre;
    }
    
    .copy-btn {
      float: right;
      padding: 5px 10px;
      background-color: #6c757d;
      color: white;
      border: none;
      border-radius: 3px;
      cursor: pointer;
      font-size: 0.8rem;
    }
    
    .copy-btn:hover {
      background-color: #5a6268;
    }
    
    .nav-tabs {
      margin-bottom: 15px;
    }
    
    .text-muted {
      font-size: 0.9rem;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1 class="mb-4">Diagnóstico de URL OAuth2</h1>
    
    <div class="alert alert-info">
      <h4>Sobre esta ferramenta</h4>
      <p>Esta página ajuda a diagnosticar problemas com URLs de autenticação OAuth2 no sistema. Use as informações abaixo para verificar se você está construindo as URLs corretamente.</p>
    </div>
    
    <div class="card mb-4">
      <div class="card-header">
        Informações básicas
      </div>
      <div class="card-body">
        <p><strong>ID da Organização:</strong> <%= organizationId %></p>
        <p><strong>Provedor:</strong> <%= provider %></p>
        <p><strong>Host:</strong> <%= host %></p>
      </div>
    </div>
    
    <div class="card mb-4">
      <div class="card-header">
        URLs corretas
      </div>
      <div class="card-body">
        <h5>Formato URL de autorização</h5>
        <div class="correct">
          <button class="copy-btn" onclick="copyToClipboard('authorize-url')">Copiar</button>
          <div id="authorize-url"><%= correctUrls.authorizeUrl %></div>
        </div>
        
        <h5>Formato URL do popup</h5>
        <div class="correct">
          <button class="copy-btn" onclick="copyToClipboard('popup-url')">Copiar</button>
          <div id="popup-url"><%= correctUrls.popupUrl %></div>
        </div>
        
        <p class="text-muted">
          <strong>Padrão correto:</strong> <%= correctUrls.correctPattern %>
        </p>
      </div>
    </div>
    
    <div class="card mb-4">
      <div class="card-header">
        URLs incorretas (não use estas!)
      </div>
      <div class="card-body">
        <h5>Formato URL de autorização incorreto</h5>
        <div class="incorrect">
          <div><%= correctUrls.deprecatedAuthorizeUrl %></div>
        </div>
        
        <h5>Formato URL do popup incorreto</h5>
        <div class="incorrect">
          <div><%= correctUrls.deprecatedPopupUrl %></div>
        </div>
        
        <p class="text-muted">
          <strong>Padrão incorreto:</strong> <%= correctUrls.incorrectPattern %>
        </p>
        
        <div class="alert alert-warning">
          <strong>Por que isso não funciona?</strong>
          <p><%= correctUrls.explanation %></p>
        </div>
      </div>
    </div>
    
    <div class="card mb-4">
      <div class="card-header">
        Exemplos de código
      </div>
      <div class="card-body">
        <ul class="nav nav-tabs" id="codeTabs" role="tablist">
          <li class="nav-item" role="presentation">
            <button class="nav-link active" id="frontend-tab" data-bs-toggle="tab" data-bs-target="#frontend" type="button" role="tab" aria-controls="frontend" aria-selected="true">Frontend (JavaScript)</button>
          </li>
          <li class="nav-item" role="presentation">
            <button class="nav-link" id="backend-tab" data-bs-toggle="tab" data-bs-target="#backend" type="button" role="tab" aria-controls="backend" aria-selected="false">Backend (Node.js)</button>
          </li>
        </ul>
        
        <div class="tab-content" id="codeTabsContent">
          <div class="tab-pane fade show active" id="frontend" role="tabpanel" aria-labelledby="frontend-tab">
            <div class="code-block">
              <button class="copy-btn" onclick="copyToClipboard('js-code')">Copiar</button>
              <pre id="js-code"><%= exampleCode.javascript %></pre>
            </div>
          </div>
          <div class="tab-pane fade" id="backend" role="tabpanel" aria-labelledby="backend-tab">
            <div class="code-block">
              <button class="copy-btn" onclick="copyToClipboard('node-code')">Copiar</button>
              <pre id="node-code"><%= exampleCode.nodejs %></pre>
            </div>
          </div>
        </div>
      </div>
    </div>
    
    <div class="card mb-4">
      <div class="card-header">
        Como testar
      </div>
      <div class="card-body">
        <p>Para testar se a configuração OAuth2 está funcionando corretamente:</p>
        
        <ol>
          <li>Clique no botão abaixo para abrir um popup de teste</li>
          <li>O popup deve abrir e iniciar o fluxo de autenticação OAuth2</li>
          <li>Se ocorrer um erro 404, a URL está incorreta e precisa ser corrigida</li>
        </ol>
        
        <button id="testBtn" class="btn btn-primary">Testar autenticação</button>
        <div id="testResult" class="mt-3" style="display: none;"></div>
      </div>
    </div>
    
    <div class="card mb-4">
      <div class="card-header">
        Resolução de problemas
      </div>
      <div class="card-body">
        <h5>Problemas comuns</h5>
        
        <div class="accordion" id="troubleshootingAccordion">
          <div class="accordion-item">
            <h2 class="accordion-header" id="headingOne">
              <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseOne" aria-expanded="false" aria-controls="collapseOne">
                Erro 404 - Página não encontrada
              </button>
            </h2>
            <div id="collapseOne" class="accordion-collapse collapse" aria-labelledby="headingOne" data-bs-parent="#troubleshootingAccordion">
              <div class="accordion-body">
                <p>Este erro ocorre quando a URL não corresponde a nenhuma rota definida no servidor.</p>
                <ul>
                  <li>Verifique se você está usando o formato correto de URL (com organizationId como parâmetro na query)</li>
                  <li>Certifique-se de que todos os arquivos que constroem URLs para o OAuth2 seguem o mesmo padrão</li>
                  <li>Se você estiver usando uma versão personalizada do frontend, atualize o código para o novo formato de URL</li>
                </ul>
              </div>
            </div>
          </div>
          
          <div class="accordion-item">
            <h2 class="accordion-header" id="headingTwo">
              <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseTwo" aria-expanded="false" aria-controls="collapseTwo">
                Erro "organizationId ausente"
              </button>
            </h2>
            <div id="collapseTwo" class="accordion-collapse collapse" aria-labelledby="headingTwo" data-bs-parent="#troubleshootingAccordion">
              <div class="accordion-body">
                <p>Este erro ocorre quando a URL está no formato correto, mas o parâmetro organizationId não foi incluído na query.</p>
                <ul>
                  <li>Verifique se você está passando o parâmetro organizationId na query</li>
                  <li>Certifique-se de que o valor do organizationId é um UUID válido</li>
                </ul>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    function copyToClipboard(elementId) {
      const element = document.getElementById(elementId);
      const text = element.innerText || element.textContent;
      
      navigator.clipboard.writeText(text).then(() => {
        const copyBtn = event.target;
        const originalText = copyBtn.innerText;
        copyBtn.innerText = 'Copiado!';
        
        setTimeout(() => {
          copyBtn.innerText = originalText;
        }, 2000);
      }).catch(err => {
        console.error('Erro ao copiar texto: ', err);
      });
    }
    
    document.getElementById('testBtn').addEventListener('click', function() {
      const resultDiv = document.getElementById('testResult');
      resultDiv.style.display = 'block';
      resultDiv.innerHTML = '<div class="spinner-border spinner-border-sm text-primary me-2" role="status"></div> Testando...';
      
      try {
        const width = 600;
        const height = 700;
        const left = (window.innerWidth - width) / 2;
        const top = (window.innerHeight - height) / 2;
        
        // Abrir popup com URL correta
        const popup = window.open('<%= correctUrls.popupUrl %>', 'oauth2TestPopup', 
          `width=${width},height=${height},left=${left},top=${top},toolbar=0,location=0,status=0,menubar=0`);
        
        if (!popup || popup.closed || typeof popup.closed === 'undefined') {
          resultDiv.innerHTML = '<div class="alert alert-danger">Erro: O popup foi bloqueado pelo navegador. Por favor, permita popups para este site.</div>';
          return;
        }
        
        resultDiv.innerHTML = '<div class="alert alert-success">Popup aberto com sucesso! Verifique se o fluxo de autenticação inicia corretamente.</div>';
        
        // Verificar fechamento do popup
        const checkPopup = setInterval(function() {
          if (!popup || popup.closed) {
            clearInterval(checkPopup);
            resultDiv.innerHTML += '<div class="alert alert-info">O popup foi fechado.</div>';
          }
        }, 1000);
      } catch (error) {
        resultDiv.innerHTML = `<div class="alert alert-danger">Erro: ${error.message}</div>`;
      }
    });
  </script>
</body>
</html> 